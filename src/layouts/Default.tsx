import React, { Fragment } from 'react';
import Sidebar from '../components/layout/Sidebar';

const Default = () => {
    return (
        <Fragment>
            <Sidebar />
        </Fragment>
    );
};

export default Default;
